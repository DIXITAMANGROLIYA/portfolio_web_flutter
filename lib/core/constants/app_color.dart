import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF6F1EC); // main beige
  static const Color surface = Color(0xFFFFFFFF);   // cards / sections

  // Text
  static const Color textPrimary = Color(0xFF2E2A28);
  static const Color textSecondary = Color(0xFF7A6F68);
  static const Color textMuted = Color(0xFF9C918A);

  // Accent / CTA
  static const Color accent = Color(0xFF9C7C6D);     // buttons / highlights
  static const Color accentSoft = Color(0xFFE6DAD3); // hover / chips

  // Borders & dividers
  static const Color border = Color(0xFFE2D7D0);

  // Status (rarely used, but good to have)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
}
